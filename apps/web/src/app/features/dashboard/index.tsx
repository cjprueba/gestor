import { OverviewDashboard } from "../../features/dashboard/_components/overview";

export default function DashboardPage() {
  return (
    <OverviewDashboard files={[]} collections={[]} onFileClick={() => { }} onCollectionClick={() => { }} />
  )
}